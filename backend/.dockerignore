# Keep the build context small and, above all, never let secrets or local-only
# tooling leak into the image.
.env
.env.*
.venv
__pycache__/
*.pyc
.pytest_cache/

# Dev-time only — not part of the runtime image.
db/
eval/
alembic.ini
